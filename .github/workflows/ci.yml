name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: swiftfilez-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[dev]'
      - name: Compile
        run: python -m compileall -q swift_files main.py
      - name: Lint
        run: |
          python -m ruff format --check swift_files tests main.py
          python -m ruff check swift_files tests main.py
      - name: Test
        run: >-
          python -m pytest
          --cov=swift_files.core
          --cov=swift_files.config
          --cov=swift_files.csv_ops
          --cov=swift_files.docx_ops
          --cov=swift_files.pdf_ops
          --cov-report=term-missing
          --cov-fail-under=80
      - name: CLI smoke
        run: |
          swf --version
          swf doctor --json

  package:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip build
      - run: python -m build

  container:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build image
        run: docker build -t swiftfilez:ci .
      - name: Container smoke
        run: docker run --rm swiftfilez:ci doctor --json

  dependency-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Audit dependencies
        run: |
          python -m pip install --upgrade pip pip-audit
          python -m pip install .
          python -m pip_audit
